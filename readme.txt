Some indications and clarifications:

Puzzle (   file: a file that contains a logipix puzzle as described in the problem   )


fillframe( frame: the frame is constructed  in the test. java file )


BrokenLines(  clue) gives an array list of string containing the paths

Stringtopath( s: takes a string that is given by broken line and turn it to an array list of clues)  